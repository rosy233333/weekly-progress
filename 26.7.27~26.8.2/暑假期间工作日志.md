# 暑假期间工作日志

对自己的要求：周一~周五每天工作6小时，有其它安排时除外。

## 7.31

- 学期总结（1.5h）

## 8.3

- 合并xjj的提交
- 对xjj的提交提出[修改方案](https://github.com/rosy233333/vsched2/blob/main/doc/%E8%B0%83%E5%BA%A6%E5%99%A8wfi%E4%BF%AE%E6%94%B9%E8%AE%A1%E5%88%92.md)
- 配置AI运行环境（Codex cli+Deepseek）
- 在AI协助下完成修改方案第1、2点的实现

另：后续所有AI生成的代码我都会进行人工审阅和测试，确保其符合我的设计，并达到和人工编写相同的质量。

## 8.4

我的生日，因此今天工作量降低一些。

- 完成修改方案第3点的实现

## 8.5

今日工作量未达预期。

- 完成AsyncOS对调度器修改的适配
- 尝试解决之前的bug，但暂无进展。

## 8.6

- 询问并获得了在保留调试信息的情况下编译AsyncOS的方法。
- 在AI协助下为AsyncOS增加了在panic时展开调用链的功能。
- 尝试解决之前的bug，但暂无进展。

## 8.7

- [栈状态转化图](../26.8.3~26.8.9/栈状态转化图.png)
- 尝试解决之前的bug，但暂无进展。

## 8.8

- 解决了其中的两个bug
  - trap_handler在让出时没有正确处理设置任务状态和关中断的关系。已经在设置任务状态前和让出后分别关/开中断。
  - 其中的一次LoadPageFault，是从waker中解析到的指针为0导致的。因此已经增加了对waker的检查。

## 8.9

- 检查了在8.8的修改后仍会发生的bug种类
- 为vdso内的编译流程增加了调试信息

## 8.10

- 解决了一个bug：0xdeadbeef在riscv中不是非法指令，而是`jal t4,-150038`，导致乱跳。因此将手写汇编中的非法指令改为0x00000000。
- 为了实现qemu将不同cpu分别输出，更新了qemu版本（7.0 -> 7.1）并解决了相关问题，并修改了qemu.mk中的qemu运行参数。

## 8.11

- 尝试解决之前的bug，但暂无进展。只能把各类异常产生的原因确定为栈数据被覆盖。

## 8.12

- 更细致地定位之前的bug：
  - 与栈和任务的释放后使用无关。即使注释掉释放语句，问题依然出现。
  - 与中断有关。只有在开中断环境下会出现该问题。

## 8.13

- LoadPageFault等Fault出现的原因已确认：是调度器回收当前栈后仍在使用，而被回收的栈又被其它核心复用，导致的栈争用。将空闲栈池改为per-cpu后，就解决了该问题。

## 8.14

- 解决了两个和trap_handler相关的bug
  - 接收到中断到进入trap_handler，中间打开了中断导致的递归创建trap_handler
  - trap_wait_queue在trap_handler处于Blocking状态时返回空指针导致的核心意料之外的休眠

## 8.15

- 仍在排查和trap_handler有关，使任务无法唤醒的bug。
- 解决了和上下文相关的内存泄漏bug。

## 8.16

- 仍在排查和trap_handler有关，使任务无法唤醒的bug。
  - 目前发现其异常地频繁触发了核心睡眠和唤醒（核间中断），暂未找到原因。
