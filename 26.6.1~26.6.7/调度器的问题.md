# 调度器的问题

1. 从线程进入时，调度器运行在线程栈上，直到执行下一个任务。如果调度器运行的同时线程被其它核心运行，则会访问同一个栈，产生同步问题。
2. trap处理任务会从队列中取出不同的trap处理，是否涉及到地址空间切换的问题？
3. 目前在一个地址空间内可能取到其它地址空间的任务（例如用户态任务被中断而进入内核），但此时调用该任务相关方法会调用本地址空间的任务方法，造成问题。
4. percpu与中断的同步问题
